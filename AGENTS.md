# AGENTS.md

## 项目目标

本项目是微信小程序项目。开发时优先保证：功能可用、代码清晰、体验稳定、易于维护、符合微信小程序规范。

## 基本原则

1. 遵循微信小程序官方开发规范。
2. 优先使用小程序原生能力，避免不必要的复杂依赖。
3. 页面、组件、接口、工具函数职责清晰。
4. 代码简单直接，避免过度封装。
5. 修改代码时保持最小改动，避免影响无关功能。
6. 如现有项目已有结构和风格，优先遵循现有实现。

## 推荐目录结构

- `pages/`：页面
- `components/`：通用组件
- `utils/`：工具函数
- `services/`：接口请求与业务 API
- `store/`：全局状态管理
- `assets/`：图片、图标等静态资源
- `config/`：配置文件
- `docs/`：需求、PRD、原型、开发记录、测试记录
- `app.js` / `app.json` / `app.wxss`：全局配置

## 开发流程

所有小程序开发任务应按以下阶段推进：

1. 需求分析讨论
2. 生成 MVP PRD
3. 生成 UI 原型
4. 开发实现
5. 测试验收

每完成一个阶段，都必须在 `docs/` 目录下创建或更新对应的 Markdown 文件，用于记录阶段结论、关键决策和待办事项。

推荐文件命名：

- `docs/01-requirement-analysis.md`
- `docs/02-mvp-prd.md`
- `docs/03-ui-prototype.md`
- `docs/04-development-log.md`
- `docs/05-test-report.md`

如果项目已有文档命名规范，优先遵循现有规范。

## 阶段要求

### 1. 需求分析讨论

在开发前先明确：

- 目标用户是谁
- 要解决什么问题
- 核心使用场景是什么
- MVP 只做哪些必要功能
- 哪些需求暂不开发

完成后更新 `docs/01-requirement-analysis.md`。

### 2. 生成 MVP PRD

PRD 应包含：

- 产品目标
- 用户角色
- 核心功能列表
- 页面结构
- 主要业务流程
- 数据字段
- 异常状态
- 非 MVP 功能

完成后更新 `docs/02-mvp-prd.md`。

### 3. 生成 UI 原型

UI 原型应包含：

- 页面清单
- 页面布局说明
- 核心组件说明
- 用户操作路径
- 空状态、加载状态、错误状态
- 主要按钮和交互反馈

完成 UI 设计文档后，应继续生成高保真 UI 原型图作为开发参考：

- 高保真图应覆盖 MVP 的核心页面和关键状态。
- 配色、组件层级、间距、字体大小应尽量接近最终小程序实现。
- 原型源文件和导出图片建议放在 `docs/prototypes/` 目录。
- 生成后必须在 `docs/03-ui-prototype.md` 中记录原型源文件和图片路径。
- 如用户指定参考风格或配色，应同步更新原型图和 `docs/03-ui-prototype.md` 中的说明。

完成后更新 `docs/03-ui-prototype.md`。

### 4. 开发实现

开发时应：

1. 先阅读相关页面、组件和接口代码。
2. 优先复用已有组件、工具函数和样式。
3. 网络请求统一封装，不要在页面中到处直接写 `wx.request`。
4. 可复用 UI 抽成组件。
5. 常量、枚举、配置项不要散落在多个文件中。
6. 不要随意修改全局配置，除非任务明确需要。
7. 不要删除已有功能，除非任务明确要求。
8. 开发过程中更新 `docs/04-development-log.md`。

## UI 与交互规范

1. 界面简洁、清晰，适合移动端。
2. 按钮、输入框、列表、弹窗等交互要有明确反馈。
3. 加载中、空状态、错误状态都要处理。
4. 表单提交前要做必要校验。
5. 操作成功或失败时，应使用合适提示。
6. 注意不同屏幕尺寸下的适配。

## 数据与接口

1. 页面私有状态放在页面 `data` 中。
2. 跨页面共享数据可使用全局状态或本地缓存。
3. 本地缓存 key 命名要清晰。
4. 不要把敏感信息长期存储在本地。
5. 所有请求应经过统一请求方法。
6. 请求应处理成功、失败、超时、无网络等情况。
7. 不要在前端暴露密钥、Token、API Key。
8. 敏感接口应通过后端或云函数调用。

## 微信能力

1. 使用微信登录、订阅消息、支付、上传图片等能力时，应遵守微信规范。
2. 权限申请应在用户触发相关功能时进行。
3. 订阅消息需要用户主动授权，不要强制打扰用户。
4. 使用相机、相册、定位等能力时，要说明用途。
5. 涉及支付、用户信息、手机号等能力时，要特别注意安全和合规。

## AI 功能规范

如项目包含 AI 能力：

1. 不要在小程序前端直接调用大模型 API。
2. API Key 必须放在后端或云函数中。
3. 用户输入和 AI 输出都应做内容安全处理。
4. AI 输出尽量使用结构化格式，方便程序解析。
5. 关键操作应允许用户确认或修改。
6. 处理 AI 请求失败、超时、结果为空等异常情况。
7. 控制调用频率和成本，避免接口被滥用。

## 测试验收

测试阶段应检查：

1. 核心流程是否可完整走通。
2. 页面跳转是否正常。
3. 表单校验是否有效。
4. 加载、空状态、错误状态是否完整。
5. 弱网、接口失败、无数据场景是否可用。
6. 是否存在明显样式错乱。
7. 是否遗留调试代码、无用变量或敏感信息。

完成后更新 `docs/05-test-report.md`。

## 安全与隐私

1. 不要提交密钥、密码、Token、私有接口地址。
2. 不要收集与功能无关的用户信息。
3. 用户数据应只用于当前功能目的。
4. 日志中不要输出敏感信息。
5. 涉及个人信息时，应遵守隐私政策和平台要求。

## 输出要求

每次完成任务后，应说明：

1. 修改了哪些文件。
2. 实现了哪些功能。
3. 更新了哪些阶段文档。
4. 需要用户手动配置的内容。
5. 未完成事项或潜在风险。
